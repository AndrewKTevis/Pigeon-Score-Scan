# Pigeon Score Scan application

This directory contains the Python application, automated tests, locked dependencies and development tools.

The product overview and setup instructions are in the repository [README](../README.md). The complete recognition boundary is documented in [docs/SCOPE.md](../docs/SCOPE.md).

Run the application from the repository root with `app/src` on `PYTHONPATH`; see [BUILDING.md](../BUILDING.md) for exact commands.
